<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Post</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="p-5 w-full min-h-screen bg-zinc-900 text-white">
      <a class="text-blue mb-5 inline-block" href="/profile"
        >Back?</a
      >

      <h5 class="mb-3">Edit Post?</h5>

      <form action="/update/<%=post._id%>" method="post">
        <textarea
          placeholder="What's on your mind?"
          class="block p-2 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md"
          name="content"
        ><%=post.content%></textarea>
        <input
          class="block px-3 py-2 bg-yellow-300 text-black rounded-md w-40 mt-2"
          type="submit"
          value="Update?"
        />
      </form>
    </div>
  </body>
</html>
